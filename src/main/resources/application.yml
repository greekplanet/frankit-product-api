spring:
  application:
    name: product-api

---
spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: "LMz2PhN13pMcEeIQZRv7rm6LeyeLp0prVQDQV8GRuj8="
  expiration: 3600000 # 1??

---
spring:
  config:
    activate:
      on-profile: mock
